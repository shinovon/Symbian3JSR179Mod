# Symbian^3 JSR 179 mod
- Adds ability to get partial updates in LocationListener (when location is not available but you can get timestamp and nmea data)
- Adds `satelliteNumInView` and `satelliteNumUsed` params in Location.getExtraInfo()
